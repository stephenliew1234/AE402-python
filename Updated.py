n=int(input("how many people are in the class")
score=[]
name=[]
lowest=100
highest=0
highestname=""
lowestname=""
summath=0
for i in range(0,2*n):
    mathname=(input("name"?))
    score.append(mathscore)
    print(score)
    if i %2==1:
        score[i]=int(score[i])
    if hightest<mathscore:
        hightest=mathscore
        highestname=mathname
        if hightest<mathscore:
            hightest=mathscore
            hightestname=mathname
    if lowest>mathscore:
       lowest=mathscore
       lowestname=mathname
     summath=summath+score[i]
     print("highest,highestname")
     print("lowest,Lowestname")
     print(summath/n)   